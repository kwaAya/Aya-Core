// TODO(unako): Update this line before the quarter changes; stale availability is worse than no date.
export const AVAILABILITY_LINE = "Currently booking Q4 builds — one rescue-and-rebuild slot and one platform build left this quarter";
export const PLATFORM_COUNT = 4;